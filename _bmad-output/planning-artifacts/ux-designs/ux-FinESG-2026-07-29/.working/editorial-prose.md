| Original Text | Revised Text | Changes |
|---|---|---|
| (DESIGN.md và EXPERIENCE.md, Decision Gate) `Quyết định đang giả định` | `Quyết định hoặc giả định` | Sửa tiêu đề cột để bao quát cả các mục đã chốt (`[DECIDED]`) lẫn các giả định đang mở. |
| (EXPERIENCE.md, Foundation) `Contract áp dụng cho responsive web: desktop là bề mặt chính, tablet là bề mặt thứ hai và màn hình hẹp giữ đầy đủ chức năng qua reflow. Không được dùng CSS viewport để suy ra “điện thoại chỉ đọc”, vì viewport hẹp có thể do zoom. Hệ thống UI là library-neutral: không hành vi nào mặc nhiên kế thừa từ shadcn/ui hay thư viện khác. DESIGN.md sở hữu visual identity/token; tài liệu này sở hữu hành vi, trạng thái, bàn phím và accessibility delta phải được giữ nguyên ở mọi thư viện triển khai.` | `Hợp đồng áp dụng cho web thích ứng: desktop là nền tảng chính, tablet là nền tảng phụ, và màn hình hẹp giữ đầy đủ chức năng bằng cách sắp xếp lại bố cục. Không dùng chiều rộng CSS viewport để suy ra chế độ “điện thoại chỉ đọc”, vì zoom có thể làm giảm chiều rộng hiệu dụng. Hệ thống UI không phụ thuộc thư viện: không hành vi nào mặc nhiên kế thừa từ shadcn/ui hoặc thư viện khác. DESIGN.md sở hữu nhận diện và token thị giác; tài liệu này sở hữu hành vi, trạng thái, thao tác bàn phím và các yêu cầu accessibility mà mọi thư viện triển khai phải giữ nguyên.` | Làm rõ phạm vi responsive, nguyên nhân không được suy luận từ viewport và ranh giới thẩm quyền giữa hai tài liệu. |
| (EXPERIENCE.md, Information Architecture) `Cấp sản phẩm chỉ có Danh sách Hồ sơ và thao tác tạo/mở/sao chép.` | `Cấp sản phẩm chỉ có Danh sách Hồ sơ và các thao tác tạo, đổi tên, mở, sao chép và xóa.` | Đồng bộ mô tả kiến trúc thông tin với đầy đủ thao tác đã quy định tại S01. |
| (EXPERIENCE.md, Foundation và UJ-3) `Sàng lọc mô phỏng: người dùng có thể dùng dữ liệu dự án giả định; giả định tương lai có thể dùng mẫu nhưng phải giữ nhãn Mô phỏng.`<br>`CFO chọn chế độ Sàng lọc mô phỏng; Kịch bản Cơ sở có dữ liệu mẫu mang nhãn Mô phỏng.` | `Sàng lọc mô phỏng: người dùng có thể dùng dữ liệu dự án giả định; các giá trị mẫu phải giữ nhãn Ước tính, còn lần chạy, kết quả và báo cáo phải mang nhãn hoặc watermark Mô phỏng.`<br>`CFO chọn chế độ Sàng lọc mô phỏng; Kịch bản Cơ sở có dữ liệu mẫu mang nhãn Ước tính.` | Phân biệt nhất quán nhãn ở cấp giá trị (`Ước tính`) với nhãn ở cấp đầu ra (`Mô phỏng`). |
| (EXPERIENCE.md, Readiness Contract) `Có, nếu các cổng khác đạt` | `Có, nếu Hồ sơ đạt trạng thái “Sẵn sàng quyết định” và các cổng chọn/xuất tại S18 đều đạt.` | Thay điều kiện vòng và mơ hồ bằng điều kiện cụ thể có thể truy vết. |
| (DESIGN.md, Contrast Contract) `Văn bản thường và placeholder có ý nghĩa đạt WCAG 2.2 AA; focus indicator và biên control chịu tải đạt tối thiểu 3:1 với nền kề.` | `Văn bản thường và placeholder có ý nghĩa phải đạt WCAG 2.2 AA; chỉ báo focus và đường biên của thành phần điều khiển thiết yếu phải có tỷ lệ tương phản tối thiểu 3:1 với nền kề.` | Làm rõ tính bắt buộc và đối tượng phải đạt tỷ lệ tương phản 3:1. |
| (DESIGN.md, CMP-19) `Tối ưu/Vô nghiệm/Khả thi chưa chứng minh tối ưu/Không vượt kiểm tra` | `Tối ưu đã kiểm chứng / Vô nghiệm / Khả thi chưa chứng minh tối ưu / Không vượt Bộ kiểm tra nghiệm` | Dùng đúng tên trạng thái chuẩn để tránh sinh thêm alias trong triển khai và kiểm thử. |
| (EXPERIENCE.md, S07) `Dự án không đạt điều kiện bị loại khỏi gói, không bị loại khỏi toàn bộ danh mục.` | `Dự án không đạt điều kiện của một Gói vay thì bị loại khỏi Gói vay đó, không bị loại khỏi toàn bộ danh mục.` | Xác định rõ phạm vi loại trừ, tránh hiểu “gói” là gói phương án đầu tư. |
| (EXPERIENCE.md, CMP-05) `lỗi/hint liên kết bằng semantics.` | `hint và lỗi được liên kết với control bằng aria-describedby và aria-errormessage.` | Thay yêu cầu chung chung bằng quan hệ ngữ nghĩa cụ thể, có thể triển khai và kiểm thử. |
| (EXPERIENCE.md, CMP-07) `Dùng contract grid tại Interaction Primitives` | `Dùng hợp đồng bảng nhập liệu HTML native tại Interaction Primitives.` | Loại bỏ từ “grid” có thể khiến đội triển khai dùng ARIA grid trái với quy định bảng native. |
| (EXPERIENCE.md, CMP-09) `modal giữ/ trả focus đúng contract.` | `modal giữ focus trong lớp phủ và trả focus về phần tử kích hoạt theo hợp đồng.` | Nêu rõ hành vi focus khi mở và đóng modal. |
| (EXPERIENCE.md, CMP-15) `Native radio/card` | `Card lựa chọn chứa native <input type="radio">.` | Làm rõ quan hệ giữa card trình bày và control radio native, tránh hai cách triển khai đối nghịch. |
| (EXPERIENCE.md, Permission-Denied Contract) `Giữ bằng chứng ở read-only; từ chối hành động dùng alert sau submit và trả focus về heading lỗi.` | `Giữ bằng chứng ở chế độ chỉ đọc. Nếu người dùng gửi yêu cầu không đủ quyền, hiển thị alert rồi trả focus về tiêu đề lỗi.` | Tách hai quy tắc độc lập và bổ sung chủ thể, thời điểm cho hành vi thông báo lỗi. |
| (EXPERIENCE.md, Permission-Denied Contract) `Nếu sự tồn tại của đối tượng nhạy cảm, trả trang Không tìm thấy/Không có quyền ở shell hiện tại; không tiết lộ tên, metadata, số lượng hoặc owner.` | `Nếu ngay cả sự tồn tại của đối tượng cũng là thông tin nhạy cảm, trả về trang Không tìm thấy/Không có quyền trong shell hiện tại; không tiết lộ tên, metadata, số lượng hoặc owner.` | Làm rõ điều kiện bảo mật để không bị hiểu thành “đối tượng nhạy cảm”. |
| (EXPERIENCE.md, Validation & Error Recovery) `CTA bị chặn nhưng người dùng cần khám phá lý do dùng focusable aria-disabled="true" với guard hành động; CTA không cần khám phá có thể dùng disabled native.` | `Khi CTA bị chặn nhưng người dùng vẫn cần khám phá lý do, dùng control có thể focus với aria-disabled="true" và guard phía xử lý hành động; khi không cần khám phá lý do, có thể dùng thuộc tính disabled native.` | Làm rõ hai trường hợp loại trừ nhau và nơi phải đặt guard để tránh chỉ vô hiệu hóa về mặt trình bày. |
| (EXPERIENCE.md, Async Feedback & Live Regions) `Lưu/tiến trình/hoàn tất dùng role="status" (polite, atomic); lỗi chặn và xung đột dùng role="alert" hoặc assertive; Dedupe theo operationId + phase.` | `Thông báo lưu, tiến trình và hoàn tất dùng role="status", aria-live="polite" và aria-atomic="true"; lỗi chặn và xung đột dùng role="alert" hoặc aria-live="assertive"; loại bỏ thông báo trùng theo operationId + phase.` | Thay mô tả ARIA dạng viết tắt bằng thuộc tính cụ thể và làm rõ cơ chế chống thông báo trùng. |
| (EXPERIENCE.md, Responsive và Accessibility) `nghiệm thu 200%/400% tới 320 CSS px`<br>`Nghiệm thu cả 200% và 400% tới 320 CSS px` | `Kiểm thử ở mức zoom 200% và 400%, đến chiều rộng tương đương 320 CSS px.` | Hợp nhất hai cách diễn đạt trùng nhau và làm rõ đây là điều kiện kiểm thử zoom kết hợp chiều rộng hiệu dụng. |
| (EXPERIENCE.md, Accessibility Contract) `Accessible name đọc đầy đủ dấu ≥/≤/−/× và viết tắt NPV/DSCR/kWh/tCO₂e.` | `Tên truy cập phải diễn đạt đầy đủ các dấu ≥, ≤, −, × và các chữ viết tắt NPV, DSCR, kWh, tCO₂e.` | Sửa chủ thể ngữ pháp: tên truy cập chứa cách diễn đạt; trình đọc màn hình mới là thành phần đọc tên đó. |
| (EXPERIENCE.md, QA-1) `mọi thay đổi ground truth tạo một phiên bản Audit Trail.`<br>`thiếu hash, quyền sử dụng hoặc ground truth khóa làm tập kiểm tra “Không hợp lệ”` | `Mọi thay đổi đối với ground truth phải tạo một phiên bản mới và một bản ghi Audit Trail.`<br>`Nếu thiếu hash, thông tin quyền sử dụng hoặc ground truth đã khóa, tập kiểm tra có trạng thái “Không hợp lệ”.` | Phân biệt phiên bản dữ liệu với bản ghi kiểm toán, đồng thời làm rõ ba điều kiện khiến tập kiểm tra không hợp lệ. |
| (EXPERIENCE.md, AC-04) `Native table S05 công bố đúng caption/hàng/cột/edit-read-only-error; Export preview dialog giữ focus, Escape đóng và trả focus.` | `Bảng HTML native tại S05 phải công bố caption, tiêu đề hàng/cột và trạng thái chỉnh sửa/chỉ đọc/lỗi; dialog xem trước xuất phải giữ focus, đóng bằng Escape và trả focus về phần tử kích hoạt.` | Tách rõ các thông tin bảng phải công bố và hoàn chỉnh vòng đời focus của dialog. |
| (EXPERIENCE.md, AC-05) `Mỗi hành động bị từ chối quyền nêu role/owner có thể xử lý, recovery path và không tiết lộ metadata nhạy cảm.` | `Mỗi hành động bị từ chối vì thiếu quyền phải nêu vai trò hoặc owner có thể xử lý và đường khắc phục, đồng thời không tiết lộ metadata nhạy cảm.` | Làm rõ nguyên nhân từ chối, người có thể xử lý và yêu cầu khắc phục. |
